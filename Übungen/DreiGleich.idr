module DreiGleich

-- Definiere einen Datentyp `DreiGleich` der angibt, dass 3 Werte gleich sind

-- data DreiGleich : ... where
--   Refl3 : DreiGleich ...
  
-- implementiere damit die Verallgemeinerung von `cong`:  

-- cong3 : { a,b,c : ty } ->{ f : ty -> ty' } -> DreiGleich ... -> DreiGleich ...

-- und "Beweise" damit: Ist x=y=z dann ist auch S x = S y = S z

-- alleGleichS : {x,y,z : Nat} -> DreiGleich ... -> DreiGleich ....
